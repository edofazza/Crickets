# crickets.yaml
#
# install: conda env create -f crickets.yaml
# update:  conda env update -f crickets.yaml
name: crickets
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - jupyter
  - nb_conda
  - pip:
    - pytest-shutil
    - scipy
    - numpy
    - matplotlib
    - pathlib
    - pandas
    - ruamel.yaml
    - pyyaml
    - opencv-python-headless
    - tensorflow
    - deap
    - sleap
    - moviepy
    - seaborn
